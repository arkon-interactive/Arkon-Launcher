# Normalise line endings so the repo stays consistent regardless of who commits.
* text=auto eol=lf

# Windows-native files keep CRLF in the working tree.
*.iss text eol=crlf
*.bat text eol=crlf

# Binary
*.ico binary
*.png binary
*.jar binary
*.zip binary
