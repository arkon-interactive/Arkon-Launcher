# Runtime
PySide6>=6.7
psutil>=5.9
requests>=2.31

# Build-time only (see build.py). Inno Setup's ISCC.exe must also be installed
# to produce the installer; it is not a pip package.
pyinstaller>=6.6
